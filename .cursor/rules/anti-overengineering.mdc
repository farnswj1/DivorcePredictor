---
description: Prevent over-engineering by keeping changes scoped, simple, and directly tied to the user's request
alwaysApply: true
---
# Anti-Over-Engineering

Only change what was asked. Simplest solution first. When unsure, ask.

Do not modify unrequested code, add abstractions without a concrete need, import
unnecessary dependencies, rewrite entire files for small changes, or add error
handling for scenarios that cannot occur.

Apply DRY only after duplication actually appears (typically a third occurrence);
do not introduce an abstraction in anticipation of reuse that may never happen.

Before delivery: verify you only changed requested code, check for simpler
approaches, and confirm no unrequested files were touched.
